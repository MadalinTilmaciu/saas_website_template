import 'package:flutter/material.dart';

class ChangelogPage extends StatelessWidget {
  const ChangelogPage({super.key});

  static const String name = 'changelog';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
